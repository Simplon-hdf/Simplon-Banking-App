doctype html
html.no-js(lang="")
    head
        meta(charset="utf-8")
        meta(http-equiv="x-ua-compatible" content="ie=edge")
        title History
        meta(name="description" content="")
        meta(name="viewport" content="width=device-width, initial-scale=1")

        link(rel="apple-touch-icon" href="apple-touch-icon.png")
        link(rel="stylesheet" href="style/main.css")

    body
        header
            h1 Virement Bancaire
        main 
            section
                form(action="/transaction/new" method='POST')
                    label Montant
                        input(type="number", name="amount", placeholder="Saisir le montant")
                    br
                    label Saisissez l'identifiant d'un de vos comptes
                        input(type="number", name="sender_id", placeholder="Identifiant de vote compte")
                    br
                    label Numéro de compte du destinataire
                        input(type="number", name="receiver_id", placeholder="Identifiant de vote destinaire")
                    br
                    button(type="submit") Valider le virement                  
